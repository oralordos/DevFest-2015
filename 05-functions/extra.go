// You can split your program into separate files, so long as everything
// is in the same folder, it will all work.
// The package statement and the imports both have to be specified per file.
// The package must be the same between all files.
package main

import "fmt"

func more() {
	fmt.Println("There is more!")
}
